# Nexus Dominion — Master Task List

Finish Phase 1 (playable alpha) and complete the Phase 2 depth systems of the
Tauri + React strategy game. Source of truth: `docs/CODING-PLAN.md` (v1.2,
re-audited 2026-07-15) for milestone intent and UAT scripts; `docs/prd.md` v3.0
and `docs/systems/*.md` for game-design specifics. Engine lives in
`src/engine/**` (pure TypeScript, vitest-covered), UI in `src/ui/**` +
`src/App.tsx` (React panels over the LCARS design system).

## Orchestrator protocol

1. **Check out** the first task with `status: TODO` whose `after:` tasks are all DONE. Set it `IN-PROGRESS`.
2. **Plan** — hand the coder the task block plus the pointers named in the intro. Nothing else.
3. **Code** — implement per the plan and the Standing constraints.
4. **Review** — check the diff against the task's **Accept** criteria (written to be mechanically checkable).
5. On pass: run the gate, commit as `<ID>: <title>`, set `status: DONE`, update this file in the same commit. On fail: one fix round, then escalate, then halt.

**Gate (every task):** `npx vitest run`, `npx tsc --noEmit`, and `npm run build` all exit 0. Zero failing tests — no exceptions, no "pre-existing" excuses.

**Human UAT gates:** Tasks with `coder: human` are playtest checkpoints. They start as `BLOCKED(human UAT)` so the runner never checks them out, and every task that lists one in `after:` stays ineligible until a human plays the checklist (see the UAT scripts in `docs/CODING-PLAN.md`), then manually flips the status to `DONE` and commits. This is deliberate: automated work must not proceed past a checkpoint the player has not signed off.

**Standing constraints** (the reviewer enforces on every task):
- Engine purity: nothing under `src/engine/**` may import from `src/ui/**`, touch the DOM, or read `localStorage` (the persistence adapter is the one sanctioned boundary).
- Every engine change ships with coverage in the sibling `*.test.ts`; every new UI behavior ships with a jsdom component test (`@testing-library/react`) asserting render + dispatched action.
- New player actions follow the established pattern: kebab-case `type` string, a `case` in `resolvePlayerActions` (`src/engine/cycle/cycle-processor.ts`), resolved atomically inside `processCycle`, with a typed event pushed to the cycle's event list.
- No new `Math.random()` in engine code — derive randomness from game state (see `simpleHash` in the cycle processor) so campaigns are reproducible.
- Build UI from the LCARS components in `src/components/lcars` (`Panel`, `Button`, `DataReadout`, `PhaseIndicator`) and match existing panel CSS conventions.
- No new runtime dependencies without an explicit note in the task body.

Statuses: `TODO` | `IN-PROGRESS` | `DONE` | `BLOCKED(reason)`

---

## M1 — War Works (combat becomes playable — CODING-PLAN 1.10/1.11)

### T-101 · Engine: `attack` player action — `status: DONE` · `coder: fable` · `after: —`
Add an `attack` case to `resolvePlayerActions` in `src/engine/cycle/cycle-processor.ts` that lets the player assault an enemy-owned system. Details: `{ targetSystemId, unitIds }` (attacking force drawn from the player's units). Resolve via the existing `resolveFullCombat` sequence in `src/engine/combat/combat-resolver.ts` (fleet engagement → orbital bombardment → ground assault, morale checks, infrastructure damage). Apply casualties to both empires' unit rosters, transfer system ownership on ground-assault victory, and push a `combat` event carrying per-phase results (casualties, victor, phases fought, ownership change) so the UI can render it. Reject invalid attacks (unowned/own target, empty or nonexistent force) by breaking without state change. Use state-derived randomness, not `Math.random()`.
**Accept:** new vitest cases in `cycle-processor.test.ts` cover: attacker victory transfers ownership; attacker defeat leaves ownership unchanged; casualties are deducted from both rosters; attacking with 0 units or against own/unclaimed system is a no-op; the pushed combat event contains phase-by-phase results. Full suite green.
**Delivered (2026-07-15):** Implemented the `attack` case in `resolvePlayerActions` (`src/engine/cycle/cycle-processor.ts`): validates the target system and force, builds attacker/defender rosters from live units and stationed fleets, runs them through the existing `resolveFullCombat` phase sequence with a state-derived `SeededRNG` (campaign seed + cycle + hashed target system id, never `Math.random()`), applies orbital-bombardment infrastructure damage, deletes casualty units from both empires' rosters and fleets, transfers system ownership on a ground-assault win, and pushes a `combat` event extended with `results`, `attackerId`/`defenderId`, `victor`, `phasesFought`, `ownershipChanged`, and per-side casualty id lists (`src/engine/types/events.ts`) while keeping the original `result` field for UI/bot back-compat. Invalid attacks (empty/nonexistent force, unclaimed/own/nonexistent target) break out with zero state mutation and no event. Nine new vitest cases in `cycle-processor.test.ts` cover every acceptance scenario plus phase-by-phase event shape. Scope boundary: no retreat/partial-force-withdrawal logic and no multi-system/simultaneous attacks — one `attack` action resolves one full engagement against one target system, matching the task's `{ targetSystemId, unitIds }` contract; UI wiring (attack button, combat modal, fleet orders) is deliberately deferred to T-102/T-103/T-104.

### T-102 · UI: attack action in System Panel — `status: DONE` · `coder: opus` · `after: T-101`
In `src/ui/SystemPanel.tsx`, when the selected system is enemy-owned, render an ATTACK section: list the player's available military units with checkboxes (or select-all), and a launch button that calls a new `onAttack(systemId, unitIds)` prop. Wire `handleAttack` in `src/App.tsx` following the existing handler pattern (dispatch `attack` through `processCycle`, advance cycle, show report). Disable the button with an inline reason when the player has no units.
**Accept:** a jsdom component test renders `SystemPanel` with an enemy-owned system fixture and asserts the attack control appears, is absent for own/unclaimed systems, and fires `onAttack` with the selected unit ids; `tsc` and full suite green.
**Delivered (2026-07-15):** Added a "Military Assault" section to `src/ui/SystemPanel.tsx`, rendered only for enemy-owned systems: it gathers the player's completed (non-in-progress) units across all of their fleets, lists each with a checkbox plus a "Select all" toggle, and a "⚔ LAUNCH ATTACK" button that calls the new `onAttack(systemId, unitIds)` prop — disabled (with an inline "No military units available to deploy" note) when the player has no eligible units, and disabled until at least one unit is checked otherwise. Wired `handleAttack` in `src/App.tsx` following the existing action-handler pattern: dispatches `{ type: "attack", details: { targetSystemId, unitIds } }` through `processCycle`, updates power-history/bot-accumulator refs and cycle state on commit, surfaces the cycle report, and closes the panel. Added matching styles to `src/ui/SystemPanel.css` and enabled `.test.tsx` discovery in `vite.config.ts` (test `include` previously matched only `.test.ts`). New jsdom component test `src/ui/SystemPanel.test.tsx` covers: attack control renders for an enemy system with units, is absent for own/unclaimed systems, is present-but-disabled with a no-units message when the player has none, select-all/individual toggles drive the checked state, and `onAttack` fires with the selected unit ids. Scope boundary: no combat-outcome UI (deferred to T-103's combat report modal) and no fleet-location/range filtering of eligible units — any completed unit in any of the player's fleets is offerable, matching the engine's `attack` action contract from T-101.

### T-103 · UI: combat result modal — `status: DONE` · `coder: opus` · `after: T-101`
New `src/ui/CombatReport.tsx` (+ CSS): a modal that renders the `combat` event from T-101 phase by phase using the LCARS `PhaseIndicator` — per phase: attacker/defender losses, morale breaks, victor; footer shows final outcome and ownership change. Wire into `src/App.tsx`: when a committed cycle's report contains a combat event involving the player, show the modal (stacked above / instead of the generic cycle report, matching existing modal conventions).
**Accept:** a jsdom component test renders the modal from a three-phase combat event fixture and asserts each phase's casualties and the final victor/ownership line are shown; modal closes via its close control; full suite green.
**Delivered (2026-07-15):** Added `src/ui/CombatReport.tsx` (+ `CombatReport.css`): a `CombatReportModal` that renders a `combat` event's `results` phase array (falling back to the single `result` when `results` is absent, for back-compat with pre-T-101 event shapes) through the LCARS `PhaseIndicator`, with one section per phase showing attacker/defender loss counts, a "Morale Break" flag when `moraleBreak`/`retreated` is set, infrastructure-damage percentage when present, and the phase victor — each empire id is tagged "(You)" when it matches the viewing player. A footer states the overall Victory/Defeat outcome and whether ownership transferred. Wired into `src/App.tsx`: derives `playerCombatEvent` from the committed `cycleReport`'s events (attacker or defender matching `state.playerEmpireId`) and renders the modal stacked above the generic `CycleReportModal`; dismissal is tracked by report object identity (`dismissedCombatReport`) so a freshly committed cycle's combat event re-opens the modal even if the previous one was dismissed. Four jsdom tests in `CombatReport.test.tsx` cover: per-phase casualty/morale rendering across a three-phase fixture, the footer's victor/ownership text, closing via the DISMISS button, closing via the Panel's × control, and the `results`-absent fallback path. Scope boundary: no animation/sequencing between phases (all render at once, matching `PhaseIndicator`'s existing static usage) and no in-modal retry/next-action controls — dismissal only, per the task's accept criteria.

### T-104 · UI: fleet movement orders — `status: DONE` · `coder: opus` · `after: T-101`
Give the player a way to issue `move-fleet` actions (engine path exists and is tested — repaired 2026-07-15). In `src/ui/MilitaryPanel.tsx`, show the player's fleets with location and in-transit status (`targetSystemId`/`arrivalCycle`); add a move control that picks a destination system and calls a new `onMoveFleet(fleetId, targetSystemId)` prop, wired in `App.tsx` to dispatch `{ type: "move-fleet", details: { fleetId, targetSystemId } }`. Show an in-transit badge until arrival.
**Accept:** a jsdom component test asserts a fleet row renders with its location, `onMoveFleet` fires with the chosen fleet + destination, and an in-transit fleet shows its arrival cycle and no move control; full suite green.
**Delivered (2026-07-15):** Added a "Fleet Deployment" section to `src/ui/MilitaryPanel.tsx` (+ `MilitaryPanel.css`) that lists the player's fleets by `empire.fleetIds`, showing each fleet's name and current system name (`state.galaxy.systems.get(fleet.locationSystemId)`). A fleet with no `targetSystemId` gets a destination `<select>` (all systems except its current location) plus a MOVE button, disabled until a destination is chosen, that calls the new `onMoveFleet(fleetId, targetSystemId)` prop; per-fleet destination choice is held in local `useState` keyed by fleet id. A fleet already in transit (`targetSystemId !== null`) instead renders an in-transit badge with the destination name and `Arrival Cyc {arrivalCycle}`, with no select/button. Wired `onMoveFleet` in `src/App.tsx` as `handleMoveFleet`, which runs `processCycle` with a single `{ type: "move-fleet", details: { fleetId, targetSystemId } }` action through the existing cycle-commit pipeline (same pattern as `handleBuildUnit`) and updates state/power-history/bot-accumulator/cycle-report on a committed result. Three jsdom tests in `MilitaryPanel.test.tsx` cover: fleet row renders with location, `onMoveFleet` fires with the chosen fleet + destination once a destination is selected, and an in-transit fleet shows its arrival cycle with no move control. Scope boundary: no map-based destination picking, multi-fleet batch orders, or in-transit cancel/redirect — one dropdown-driven move order per fleet, matching the task's `onMoveFleet(fleetId, targetSystemId)` contract; combat-on-arrival and ownership effects remain the engine's/T-101's concern.

### U-110 · HUMAN UAT: "War works" playthrough — `status: BLOCKED(human UAT)` · `coder: human` · `after: T-111, T-112, T-113, T-114, T-115, T-116, T-117`
Play the game (`npm run dev` or `npm run tauri dev`): start a campaign, build units over several cycles, move a fleet, attack a bot's system, lose a fight, win a fight, watch the combat modal each time, and confirm the map recolors on conquest. UAT scripts: CODING-PLAN milestones 1.10/1.11. Record verdicts and any change requests directly in this file, add follow-up tasks for them, then set this task `DONE`.
**Accept:** human sign-off recorded here (date + notes); status flipped to DONE by a human commit.
**Verdict (2026-07-16): FAILED.** Player findings: (1) star systems render on top of each other — board unreadable; (2) no onboarding / promised skippable tutorial absent; (3) no indication of turn structure or currently-available actions; (4) 60 empty COMMIT CYCLE clicks advanced rank with zero game actions. Root causes traced to design-doc violations (PRD:158 queue-then-commit; FRONTEND-DESIGN §3.1 phase indicator; PROGRESSIVE-SYSTEMS §2 directed start; no min-distance in galaxy gen). Remediation: milestone M1.5 below; this task now retests after it.

---

## M1.5 — UAT Remediation: readable board, orders-queue turns, tutorial (U-110 follow-ups)

### T-111 · Engine: minimum inter-system distance in galaxy generation — `status: DONE` · `coder: opus` · `after: —`
In `src/engine/galaxy/galaxy-generator.ts`, enforce a global minimum distance of 28 world units (= 2 × StarMap's 14-unit HIT_RADIUS, making clicks unambiguous) between all systems. Keep the existing per-system angle/radius sampling and single `SeededRNG` stream, but retry each placement up to 40 attempts, accepting the first candidate ≥ 28 units from every previously placed system across ALL sectors (sector fringes interleave — the check must be global, not per-sector). If all attempts fail, place the candidate with the largest nearest-neighbour distance so generation never fails and counts stay exactly 25/sector. `buildAdjacency`, cross-sector links, and homeworld assignment are unchanged (they operate on final positions). Naive O(placed × attempts) distance scan is fine (<50ms for 250 systems).
**Accept:** new tests in `galaxy-generator.test.ts`: (a) for seeds 42, 7, and 12345 with the default 250-system config, minimum pairwise distance across all systems ≥ 28; (b) the adjacency graph is fully connected (BFS from one system reaches all 250); (c) same seed → identical positions. All existing generator tests pass unmodified. Full suite, `tsc`, build green.

**Delivered (2026-07-16):** Added a global `placedPositions` accumulator spanning all sectors and a retry loop around each system's angle/radius sampling in `galaxy-generator.ts`, accepting the first candidate whose nearest-neighbour distance across every previously placed system (not just the current sector) meets `MIN_SYSTEM_DISTANCE = 28`, with a largest-nearest-neighbour fallback so generation can never fail and per-sector counts stay exactly 25. Scope boundary: the retry cap was raised from the spec's 40 to `MAX_PLACEMENT_ATTEMPTS = 250` — at 25 systems/sector packed into the radius-weighted disc, 40 attempts sits near the geometric feasibility limit for 28-unit separation and caused the fallback to fire often enough to drag the measured global minimum below 28 for seeds 7 and 12345 (~22–25), failing the acceptance criterion; 250 attempts clears all three acceptance seeds (28.00–28.09 measured) while generation stays ~15–23ms, well inside the 50ms budget, and the deviation plus rationale is documented inline in the source. `buildAdjacency`, cross-sector links, and homeworld assignment were left untouched as specified. New tests cover the three acceptance criteria (min pairwise distance ≥ 28 for seeds 42/7/12345, full BFS connectivity, and positional determinism across repeated generation with the same seed); all existing generator tests pass unmodified.

### T-112 · UI: StarMap node scaling, hit radius, and label thresholds — `status: DONE` · `coder: opus` · `after: T-111`
In `src/ui/StarMap.tsx`, extract two exported pure helpers (same pattern as the existing exported `computeCovenantEdges`): `nodeScreenRadius(baseRadius, zoom)` using a zoom multiplier clamped to [0.35, 1.5] instead of today's [0.6, 1.5] (so nodes shrink when zooming out and, with T-111 spacing, never overlap at any zoom), and `hitRadiusWorld(zoom)` returning `max(14, 8 / zoom)` (clicks stay usable when zoomed out; existing nearest-wins click resolution unchanged). Use both in the render loop and click handler; scale the selection ring with the same clamp. Lower the system-name label threshold from `zoom > 1.2` to `zoom > 1.0`.
**Accept:** unit tests for both helpers: monotonic in zoom, spot values at zoom 0.15 / 0.8 / 4.0, and `hitRadiusWorld(0.8) === 14`. All existing StarMap tests pass. Full suite, `tsc`, build green.

**Delivered (2026-07-16):** Extracted `nodeScreenRadius(baseRadius, zoom)` and `hitRadiusWorld(zoom)` as exported pure helpers in `StarMap.tsx`, mirroring the existing `computeCovenantEdges` pattern. `nodeScreenRadius` widens the zoom-multiplier clamp from `[0.6, 1.5]` to `[0.35, 1.5]` so nodes keep shrinking down to minimum zoom instead of floor­ing early, and is now used for both node rendering and the selection ring (replacing the old inline `SELECTED_RING * camera.zoom` calculation). `hitRadiusWorld` returns `max(14, 8 / zoom)`, replacing the fixed `HIT_RADIUS = 14` constant in the click handler; since `useCanvasInteraction`'s `camera.zoom` is only available after the hook call while the click handler is defined before it, a `cameraZoomRef` ref was added and kept in sync each render so the handler always reads the current zoom without re-creating the callback. The system-name label threshold was lowered from `zoom > 1.2` to `zoom > 1.0`. New tests in `src/ui/starmap-scaling.test.ts` cover monotonicity and the required spot values (0.15/0.8/4.0) for both helpers, including `hitRadiusWorld(0.8) === 14`; all existing StarMap tests were left unmodified and pass. Scope boundary: only the two specified helpers, their call sites, and the label threshold were touched — node base radii (`NODE_RADIUS`, `NODE_RADIUS_HOME`), colour logic, and the nearest-wins click resolution algorithm itself were left as-is per the task's explicit "unchanged" instructions.

### T-113 · UI: orders queue — queue actions, commit once (the turn-model refactor) — `status: DONE` · `coder: fable` · `after: —`
Refactor `src/App.tsx` from commit-per-action to queue-then-commit per PRD:158. New pure module `src/ui/orders.ts`: `QueuedOrder { id, type, details, label, dedupeKey }` with `enqueueOrder` / `removeOrder` / `toEngineActions`; table-driven dedupe rules: `claim-system`/`attack` share key `system-intent:${systemId}` (last-wins across the family — an attack replaces a queued colonise of the same system); `trade:${resource}:${direction}` and `fund-syndicate` merge quantities; `build-unit` is stackable (null key); `move-fleet:${fleetId}`, `pact:${targetId}`, `covert:${targetId}`, `research`, `doctrine`, `specialization`, `wormhole:${targetSystemId}` are last-wins; `break-pact:${pactId}` and `br:${itemId}` are idempotent. All ~15 action handlers in App.tsx change from `commitActions([action])` to enqueueing (human-readable labels built from state at enqueue time; handlers keep side-effects like closing panels). COMMIT CYCLE sends `toEngineActions(orders)` through the existing `commitActions`; on success clear the queue and show the one CycleReport as today; on failure keep the queue and show the existing CommitErrorBanner. Committing an EMPTY queue stays legal (pass) but requires a two-click confirm ("CONFIRM: PASS CYCLE", auto-revert after 3s; no `window.confirm`). New `src/ui/OrdersQueue.tsx` + CSS: collapsible LCARS tray docked below the HUD top bar showing each order's label with a remove (✕) button, "×N" grouping for stacked build-units, CLEAR ALL, and "NO ORDERS QUEUED" when empty. Orders live in React state only (not GameState, not saved — document this).
**Accept:** unit tests in `orders.test.ts` covering every dedupe rule above; jsdom integration test: a panel action does NOT change the cycle number or resources and appears in the tray; COMMIT advances the cycle exactly once, shows one report, clears the queue; empty-queue commit requires two clicks (one click does not advance); a removed order is not executed; a failed commit (mocked `committed: false`) preserves the queue and shows the banner. Existing panel tests updated from asserting immediate state change to asserting enqueue. Full suite, `tsc`, build green.

**Delivered (2026-07-16):** Added the pure `src/ui/orders.ts` module (`QueuedOrder`, `enqueueOrder`/`removeOrder`/`toEngineActions`, table-driven `RULES` registry) implementing every dedupe family from the spec — `claim-system`/`attack` sharing `system-intent:${systemId}` (last-wins), `trade`/`fund-syndicate` quantity merges, stackable `build-unit`, last-wins singles for move-fleet/pact/covert/research/doctrine/specialization/wormhole, and idempotent break-pact/black-register — plus `src/ui/OrdersQueue.tsx` + CSS, a collapsible LCARS tray below the HUD top bar with per-order remove, `×N` build-unit grouping, CLEAR ALL, and an empty-state message. All ~15 action handlers in `src/App.tsx` were converted from immediate `commitActions([action])` calls to `enqueue(...)` with state-derived human-readable labels; `handleCommitCycle` now flushes `toEngineActions(orders)` through the existing `commitActions`, clearing the queue on success and preserving it with the existing `CommitErrorBanner` on failure, and an empty-queue commit requires a two-click confirm (`CONFIRM: PASS CYCLE`, auto-reverting after 3s, no `window.confirm`) surfaced via the HUD commit button's label/variant. Test coverage: `src/ui/orders.test.ts` exercises every dedupe rule in the table, and `src/App.test.tsx` covers the jsdom integration paths (panel action enqueues without advancing the cycle, COMMIT advances exactly once and clears the queue, two-click empty pass, order removal, and a failed-commit queue-preservation case). Scope boundary: `build-installation` dedupe was not specified in the task body — it was assigned an idempotent `install:${systemId}:${installationType}` key (flagged inline in `orders.ts` as a planner decision, one line to switch to stackable if reviewed otherwise); no other action semantics, engine changes, or persistence of the queue were introduced.

### T-114 · UI: queued-state badges in panels — `status: DONE` · `coder: opus` · `after: T-113`
Add `summarizeOrders(orders): OrdersSummary` to `src/ui/orders.ts` (`systemIntents: Map<systemId, "colonise"|"attack"|"wormhole">`, `buildUnitCounts: Map<unitTypeId, number>`, `trades[]`, `researchQueued`, `doctrineQueued`, `totalOrders`), computed once in App with `useMemo` and passed to panels. MarketPanel renders "PENDING: BUY 50 FOOD"-style rows; MilitaryPanel shows "QUEUED ×N" chips on unit rows; SystemPanel shows a "COLONISATION QUEUED" / "ATTACK QUEUED" banner and disables that system's action button; ResearchPanel marks the queued doctrine/specialisation. Deliberately NO resource-cost pre-simulation: displayed resources are real reserves; the engine validates affordability at commit (document this limitation in a code comment).
**Accept:** `summarizeOrders` unit tests (grouping, counts, family intents); jsdom tests: MarketPanel pending-trade row, MilitaryPanel queued chip, SystemPanel queued banner + disabled button, each driven by a summary fixture. Full suite, `tsc`, build green.

**Delivered (2026-07-16):** Added `summarizeOrders(orders): OrdersSummary` to `src/ui/orders.ts`, a pure reducer over the queue producing `systemIntents` (last-write-wins per system across the `claim-system`/`attack`/`build-wormhole` types), `buildUnitCounts`, merged `trades[]` (with prebuilt display labels), `researchQueued`, `doctrineQueued`, a `specializationQueued` field added beyond the task's listed fields so ResearchPanel could mark the queued specialisation as its accept line requires, and `totalOrders`. `App.tsx` computes it once via `useMemo(() => summarizeOrders(orders), [orders])` and threads it into all four panels as a new `summary` prop. MarketPanel renders "PENDING: {label}" rows above the trade interface for each queued trade; MilitaryPanel shows a "QUEUED ×N" chip on each build-unit row; SystemPanel shows a "COLONISATION QUEUED" / "ATTACK QUEUED" / "WORMHOLE QUEUED" banner keyed off `systemIntents.get(systemId)` and disables the matching action button (colonise, attack, build-wormhole) to prevent re-queuing the same intent; ResearchPanel tags the queued doctrine path and specialisation with a "QUEUED" pill and a "TIER ADVANCE QUEUED" note next to the advance button. The no-cost-pre-simulation limitation is documented inline in `summarizeOrders`'s doc comment: panels always show real empire reserves, never a projected post-queue balance, and the engine remains the sole affordability check at commit. Test coverage: unit tests for `summarizeOrders` (grouping, counts, last-write-wins intents) plus new/updated jsdom tests in `MarketPanel.test.tsx` (new), `ResearchPanel.test.tsx` (new), `MilitaryPanel.test.tsx`, and `SystemPanel.test.tsx`, each driven by a summary fixture. Scope boundary: badges are purely presentational reads of the queue — no changes to dedupe rules, `toEngineActions`, or commit-cycle behaviour from T-113; `totalOrders` is exposed on the summary but not yet consumed by any panel (reserved for T-115's HUD commit-button count, out of scope here).

### T-115 · UI: TurnStatus HUD — cycle/Reckoning readout + available-actions hint — `status: DONE` · `coder: opus` · `after: T-113`
New `src/ui/TurnStatus.tsx` (+ CSS) embedded in the HUD top bar, replacing the unlabeled confluence pips. Displays: `CYCLE {time.currentCycle} · CONFLUENCE {time.currentConfluence} · RECKONING IN {time.cyclesUntilReckoning} · {TIER}` (all fields already exist on `state.time` — no engine change), the labeled pip strip, and the existing LCARS `PhaseIndicator` in a compact 3-beat loop ORDERS → RESOLUTION → REPORT (REPORT highlighted while the CycleReport modal is open; this is a documented simplification — the engine's 17 phases are not player-visible). The COMMIT CYCLE button label gains the queued-order count: `⟫ COMMIT CYCLE (N)`. A chevron expands a dropdown listing currently-available action categories derived from cheap state checks — Colonise (an unclaimed system adjacent to player territory exists), Build (owned system has open installation slots), Military (player can afford the cheapest unit), Attack (player has completed units and a known enemy system), Trade (always), Research (research points > 0) — each row naming the panel that performs it.
**Accept:** jsdom tests: exact readout string for a fixture state; order count renders on the commit button; expanding shows the action list; the Colonise row is present for a fixture with an adjacent unclaimed system and absent when there is none. Full suite, `tsc`, build green.

**Delivered (2026-07-16):** Added `src/ui/TurnStatus.tsx` (+ `TurnStatus.css`), a self-contained component now embedded in `HUD.tsx`'s top bar in place of the old unlabeled `hud__cycle-info` block (its CSS rules removed wholesale from `HUD.css`). It renders the single-text-node readout `CYCLE {n} · CONFLUENCE {n} · RECKONING IN {n} · {TIER}` (kept as one string, not interpolated JSX fragments, so the exact-string jsdom assertion holds), a labeled 10-pip confluence strip, the existing LCARS `PhaseIndicator` collapsed to a 3-beat ORDERS → RESOLUTION → REPORT loop (REPORT highlights via a new `reportOpen` prop threaded from `App.tsx` as `cycleReport !== null`), the order-count-bearing commit button (`⟫ COMMIT CYCLE (N)` / `⟫ PASS CYCLE` / `CONFIRM: PASS CYCLE`, logic moved verbatim from the old `HUD.tsx`), and a chevron-toggled dropdown driven by a pure `availableActions(state)` helper covering all six specified categories (Colonise, Build, Military, Attack, Trade-always, Research) with cheap state checks and panel-name hints. `App.test.tsx`'s `cycleNumber` helper was updated to read the new `data-testid="turn-status-readout"` node instead of the retired `.hud__cycle-number` class. Scope boundary: this is a read-only status/hint surface — `availableActions` performs advisory checks only, not authoritative validation, and no engine files or action-dispatch logic were touched, per the task's explicit "no engine change" framing.

### T-116 · Engine: tutorial state, directed-start objectives, bot passivity — `status: DONE` · `coder: fable` · `after: —`
New `src/engine/tutorial/tutorial-engine.ts` + tests. Optional plain-JSON field on GameState: `tutorial?: TutorialState { active, objectiveIndex, completed: string[], signals: string[], baselineUnitCount, skipped }` (plain arrays/objects only — the generic state serializer then needs no changes; old saves deserialize with `tutorial` undefined). `createNewCampaign(config, name, options?: { tutorial?: boolean })` creates it on opt-in. Five objectives completed strictly in order, evaluated by `advanceTutorial(state)` called once late in `processCycle` Tier-1: (1) `explore` — completed via UI-reported signal recorded by `markTutorialSignal(state, "explored")`; (2) `expand` — player `systemIds.length ≥ 2`; (3) `military` — player unit count (including build queue) > `baselineUnitCount`; (4) `market` — `markTutorialAction(state, "market")` called at the `trade` case's success point in the cycle processor; (5) `combat-prep` — action-marked at the `move-fleet`/`attack` success points. Tutorial hard-deactivates after cycle 10 regardless of progress. `skipTutorial(state)` pure helper returns state with `{ active: false, skipped: true }`. `isTutorialProtectionActive(state)` guards bot passivity: extend `CombatOptions` in `src/engine/combat/combat-resolver.ts` with `defenderCasualtyMultiplier?: number` applied to the defender casualty ratio AND infrastructure damage (verify interaction with the `Math.max(1, floor(n × ratio))` casualty floor so small forces actually see reduction); in `src/engine/bot/bot-engine.ts` `executeAttack`, merge `{ defenderCasualtyMultiplier: 0.5 }` into combat options when protection is active AND the defender is the player. No localStorage in the engine; no new GameEvent types.
**Accept:** vitest: campaign with `{tutorial: true}` starts active at objective 0 and without it `state.tutorial` is undefined; each objective completes from a constructed fixture and NOT before its turn (in-order enforcement); deactivation after cycle 10; skip helper works; combat comparison test — identical seed/forces with vs without protection shows lower player casualties and infrastructure damage, unchanged attacker losses, unaffected bot-vs-bot combat, and verified reduction with small forces (floor case); a serialized pre-change state fixture (no `tutorial` key) deserializes and processes one cycle cleanly. Full suite, `tsc`, build green.

**Delivered (2026-07-16):** Added `src/engine/tutorial/tutorial-engine.ts` (+ tests), a pure module exporting `createTutorialState`, `markTutorialSignal`/`markTutorialAction`, `skipTutorial`, `isTutorialProtectionActive`, and `advanceTutorial`, plus the plain-JSON `TutorialState` field on `GameState` (`active, objectiveIndex, completed, signals, baselineUnitCount, skipped`) so the generic serializer needs no changes and old saves deserialize with `tutorial` undefined. `createNewCampaign` gained an opt-in `options?: { tutorial?: boolean }` parameter. The five objectives (explore, expand, military, market, combat-prep) are evaluated strictly in order by `advanceTutorial`, called once late in `processCycle` Tier-1, and the tutorial hard-deactivates on/after cycle 10. `resolvePlayerActions` now calls `markTutorialAction` at the trade, attack, and move-fleet success points for player-owned actions. Bot passivity is implemented via a new `CombatOptions.defenderCasualtyMultiplier` in `combat-resolver.ts`: rather than scaling the casualty ratio (which the existing `Math.max(1, floor(n × ratio))` floor would neutralize for small forces), it trims the already-selected defender casualty list and infrastructure-damage value directly, so a 1-casualty fight actually drops to 0 under protection; `bot-engine.ts`'s `executeAttack` merges `{ defenderCasualtyMultiplier: 0.5 }` into combat options only when `isTutorialProtectionActive(state)` is true and the defender is the player, leaving bot-vs-bot combat untouched. Scope boundary: this ships engine logic only, per the task's framing — no localStorage, no new `GameEvent` types, and no UI overlay or first-play flag (both are T-117's explicit responsibility).

### T-117 · UI: tutorial overlay + first-play flag — `status: DONE` · `coder: opus` · `after: T-113, T-116`
New `src/ui/TutorialOverlay.tsx` (+ CSS) and `src/ui/firstPlay.ts` (localStorage helpers `isFirstPlay()` / `markFirstPlayDone()` on key `nexus-dominion-first-play-complete` — UI-side only, engine stays pure). `handleNewCampaign` in App.tsx passes `{ tutorial: isFirstPlay() }` to `createNewCampaign` and calls `markFirstPlayDone()` immediately (abandoning the first campaign doesn't re-trigger; an in-progress tutorial persists through saves via GameState). Overlay renders when `state.tutorial?.active`: blue-tinted LCARS panel fixed in the left column above the OrdersQueue tray (never covering the map centre, per PROGRESSIVE-SYSTEMS §6), with a "DIRECTED START n/5" header, a 5-item checklist (completed = filled pip, current = highlighted with instruction text referencing the queue flow, future = dimmed), a collapse toggle, and SKIP TUTORIAL with a two-click confirm that dispatches `SET_STATE` with `skipTutorial(state)`. Objective 1 wiring: when tutorial is active at objective 0 and the player selects a system, App dispatches `SET_STATE` with `markTutorialSignal(state, "explored")` (checklist immediately shows the pending tick; the engine confirms it on next commit).
**Accept:** jsdom: with clean localStorage, NEW CAMPAIGN yields active tutorial + visible overlay; a second NEW CAMPAIGN (flag set) yields neither; selecting a system at objective 0 records the signal and updates the checklist; SKIP requires two clicks, hides the overlay, sets `skipped: true`; overlay absent when `state.tutorial` is undefined (old save); collapse toggle works; the overlay's fixed-position class is asserted. Full suite, `tsc`, build green.

**Delivered (2026-07-16):** Added `src/ui/TutorialOverlay.tsx` (+ `TutorialOverlay.css`), a purely-presentational, blue-tinted LCARS panel anchored to the left column above the OrdersQueue tray and clear of the map centre, reflecting the engine's `TutorialState` (now exported from `src/engine/types/index.ts`) via a static `ITEMS` table keyed to `TUTORIAL_OBJECTIVES`: a "DIRECTED START n/5" header, a 5-item checklist with `done`/`current`/`current-pending`/`future` pip states (the `current-pending` state renders instantly when the UI has reported a satisfying signal but the engine hasn't confirmed it on the next commit yet), a HIDE/SHOW CHECKLIST collapse toggle, and a SKIP TUTORIAL button that requires a second CONFIRM SKIP click before calling `onSkip`. Added `src/ui/firstPlay.ts`: `isFirstPlay()`/`markFirstPlayDone()` localStorage helpers on key `nexus-dominion-first-play-complete`, failing safe to "first play" when storage is unavailable. Wired into `src/App.tsx`: `handleNewCampaign` reads `isFirstPlay()`, passes `{ tutorial }` to `createNewCampaign`, and calls `markFirstPlayDone()` immediately so abandoning the first campaign doesn't re-trigger it; the overlay renders when `state.tutorial?.active` and dispatches `SET_STATE` with `skipTutorial(state)` on confirmed skip; the map's `onSelectSystem` handler, at tutorial objective 0 with the "explored" signal not yet recorded, clones state (`markTutorialSignal` mutates in place) and dispatches `SET_STATE` so React state is never mutated directly. Test coverage: `TutorialOverlay.test.tsx` (checklist/pip states, collapse, two-click skip, fixed-position class) and `App.tutorial.test.tsx` (first-campaign vs. second-campaign flag behavior, objective-0 signal wiring, overlay absence on old saves). Scope boundary: this task only wires objective 1 ("explore")'s UI signal per its explicit framing — objectives 2 through 5 (expand/military/market/combat-prep) are confirmed purely by existing gameplay actions flowing through the engine's `markTutorialAction` calls from T-116, with no additional UI-side signal wiring needed or added for them.

---

## M2 — Phase 1 Completeness (information & lifecycle gaps)

### T-201 · UI: Sector panel — `status: DONE` · `coder: opus` · `after: —`
New `src/ui/SectorPanel.tsx` (+ CSS) per CODING-PLAN 1.2: clicking a sector (add a sector-select affordance to `src/ui/StarMap.tsx` — e.g. clicking the sector hull/label) opens a slide-in listing the sector's 25 systems with owner, plus an ownership breakdown by empire and dominance status. Dismiss via X, backdrop, or Escape, matching `SystemPanel` conventions.
**Accept:** a jsdom component test renders the panel from a sector fixture and asserts the system count, per-empire ownership tallies, and dominance line; panel close control fires; full suite green.

**Delivered (2026-07-15):** Added `src/ui/SectorPanel.tsx` + `SectorPanel.css`, a slide-in listing a sector's 25 systems with owner, a per-empire ownership tally, and a dominance line, dismissible via X, backdrop click, or Escape (matching `SystemPanel` conventions). Sector selection is driven by a new `pointInPolygon` ray-cast hit test in `src/ui/StarMap.tsx`: a click that misses every system now tests the precomputed sector hulls and fires a new `onSelectSector` callback, wired up in `src/App.tsx` alongside a `selectedSectorId` state slice that mutually clears against the existing system selection. Ownership/dominance math lives in a new pure module `src/engine/galaxy/sector-dominance.ts` (`computeSectorDominance`, `SECTOR_DOMINANCE_THRESHOLD = 13`), kept separate from the UI so it's independently unit-tested. Test coverage added: `sector-dominance.test.ts` (engine math), `SectorPanel.test.tsx` (component fixture rendering + close paths), `StarMap.test.tsx` and `starmap-hit.test.ts` (click routing and polygon hit-testing). Scope boundary: this task only wires sector *selection and read-only display* — no sector-level actions (e.g. bulk fleet orders, sector-wide diplomacy) were added, and the dominance threshold is a fixed constant rather than a configurable game-balance value; both are out of scope for T-201 per CODING-PLAN 1.2.

### T-202 · UI: covenant lines + coalition alert — `status: DONE` · `coder: opus` · `after: —`
Two diplomacy visibility gaps (CODING-PLAN 1.13/1.14): (a) render Covenant Lines on `src/ui/StarMap.tsx` — an edge between home systems of empires holding an active star-covenant with the player, visually distinct from wormhole edges; (b) when a committed cycle's events contain a convergence alert, surface it as a prominent HUD banner or modal naming the empire and threatened achievement, and show the player's coalition memberships in `DiplomacyPanel`.
**Accept:** component tests assert (a) a covenant edge is emitted for an active covenant fixture and absent otherwise, and (b) the alert renders from a convergence-event fixture and DiplomacyPanel lists a coalition fixture; full suite green.

**Delivered (2026-07-15):** Added Covenant Lines to `src/ui/StarMap.tsx` via a new pure, independently-tested helper `computeCovenantEdges` (covered by `starmap-covenant.test.ts`) that walks active `star-covenant` pacts involving the player and emits an edge from the player's home system to each covenant partner's home system; rendered as amber dashed/glowing strokes clearly distinct from the existing solid purple wormhole edges, wired through new optional `empires`/`diplomacy` props threaded from `src/App.tsx`. Added a new `src/ui/ConvergenceAlert.tsx` (+ CSS) HUD banner: `App.tsx` scans the committed cycle report for a `convergence-alert` event, resolves the empire and threatened achievement names, and shows a dismissible banner keyed off report identity so a fresh report re-triggers it (`ConvergenceAlert.test.tsx`). `DiplomacyPanel` gained a "Coalition Memberships" section listing every active coalition the player belongs to, with target empire name, member count, and combat bonus (`DiplomacyPanel.test.tsx`). Scope boundary: no new player-facing actions were added (no way to join/leave a coalition or covenant from these views — that's existing diplomacy-action surface, untouched); the convergence banner surfaces only the single first matching event per cycle report rather than stacking multiple simultaneous alerts, deemed sufficient since convergence alerts are rare per CODING-PLAN 1.14.

### T-203 · UI: save-slot manager — `status: DONE` · `coder: opus` · `after: —`
Replace the load-latest-only behavior in `src/App.tsx` (CODING-PLAN 1.3 limitation). Title screen CONTINUE opens a save list from `persistence.listSaves()`: name, cycle, timestamp; pick one to load, delete with confirm. In-game SAVE keeps working; add save metadata (campaign name + cycle) if the persistence layer lacks it.
**Accept:** a jsdom component test renders the save list from a multi-save fixture, asserts load fires with the chosen save id and delete asks for confirmation; a persistence unit test covers list/load/delete round-trip; full suite green.

**Delivered (2026-07-15):** Added `src/ui/SaveSlotManager.tsx` + `SaveSlotManager.css`, a title-screen slide-in listing every save (name, cycle, timestamp) with a two-step arm/confirm delete per row and a load-by-id action, replacing the old load-latest-only shortcut. `src/App.tsx`'s CONTINUE button now calls a new `handleOpenSaveList` (populates state from `persistence.listSaves()`) instead of auto-loading the newest save, and wires new `handleLoadSave`/`handleDeleteSave` handlers into the panel; in-game SAVE is untouched. Extended `IStatePersistence.listSaves()` and both implementations (`LocalStoragePersistence`, `InMemoryStatePersistence`) to include a `cycle: number` field sourced from `state.time.currentCycle`, since the interface previously exposed only `id`/`name`/`savedAt`. Test coverage added: `SaveSlotManager.test.tsx` (renders a multi-save fixture, asserts load fires with the chosen id, asserts delete requires the confirm click) and `local-storage-persistence.test.ts` (list/load/delete round-trip including the new cycle field), plus an existing-suite extension in `state-persistence.test.ts`. Scope boundary: no save renaming, sorting/filtering controls, or multi-select bulk delete were added — the manager surfaces exactly the list/load/delete round-trip called for by CODING-PLAN 1.3; save-slot capacity limits and cloud/remote persistence backends remain out of scope.

### T-204 · Engine: surface cycle-commit errors — `status: DONE` · `coder: fable` · `after: —`
Kill the silent-failure tech debt flagged in CODING-PLAN 1.5. In `src/engine/cycle/cycle-processor.ts`, the Tier-1 atomicity `catch` must capture the thrown error into a new optional `error: string` field on `CycleResult` (state rollback behavior unchanged). In `src/App.tsx`, every handler that checks `result.committed` must, on failure, surface the error to the player (LCARS-styled toast or banner — one shared mechanism, not thirteen copies: extract the repeated commit-handling block into a single helper while you're there).
**Accept:** a vitest case forces a Tier-1 throw and asserts `committed === false` and `error` contains the message while returned state deep-equals the input; a component/unit test asserts the UI error surface renders from a failed-commit result; the commit-handling boilerplate in `App.tsx` appears once, not per-handler; full suite green.

**Delivered (2026-07-15):** `CycleResult` gained an optional `error?: string` field, populated in `processCycle`'s Tier-1 atomic `catch` block (`err instanceof Error ? err.message : String(err)`) while the rollback-to-`currentState` behavior is unchanged. `src/App.tsx` had its thirteen near-identical commit handlers (cycle commit, colonise, attack, build-installation, move-fleet, fund-syndicate, purchase-black-register, launch-covert-op, etc.) collapsed into a single `commitActions(actions, onCommit?)` helper that snapshots resources, runs `processCycle`, and on success dispatches `ADVANCE_CYCLE` + clears any prior error (invoking the optional `onCommit` for per-handler side effects like closing a panel) or, on failure, sets a new `commitError` state instead of failing silently. A new `src/ui/CommitErrorBanner.tsx` (+ `.css`) renders that `commitError` as a dismissible LCARS-styled HUD banner (`role="alert"`), wired in once at the top level of `App.tsx` rather than per-handler. Test coverage: `cycle-processor.error.test.ts` forces a Tier-1 throw and asserts `committed === false`, `error` contains the thrown message, and the returned state deep-equals the input; `CommitErrorBanner.test.tsx` asserts the banner renders the message and dismiss fires the callback; `cycle-test-fixtures.ts` was added to share cycle-processor test scaffolding between the existing and new test files. Scope boundary: no retry/undo affordance was added for a failed commit and no error taxonomy/classification beyond the raw thrown message — the banner surfaces exactly what the engine threw, one dismiss action, nothing more.

### T-205 · Engine: seed the cycle processor's stray randomness — `status: DONE` · `coder: opus` · `after: —`
Three `Math.random()` calls remain in `src/engine/cycle/cycle-processor.ts` (wormhole id at ~line 632, covert success at ~line 801, covert detection kind at ~line 820), breaking campaign reproducibility. Replace them with deterministic state-derived values following the existing `simpleHash` pattern (hash of empire id + cycle + op type, etc.). Covert probabilities must keep their intended odds (70% success; 50/50 detected-vs-failed) across the input distribution.
**Accept:** `grep -rn "Math.random" src/engine --include="*.ts" | grep -v test` outputs nothing; vitest asserts two identical states produce identical outcomes for the affected paths, and a spread of ~100 hash inputs lands within ±10 points of the 70% success rate; full suite green.

**Delivered (2026-07-15):** Replaced all three remaining `Math.random()` calls in `cycle-processor.ts` with deterministic, state-derived values. The wormhole id now uses `simpleHash` of empire id + cycle + target system id instead of `Math.random().toString(36)`. Covert-operation success and detection-kind rolls now draw from a single `SeededRNG` seeded with campaign seed + cycle + a hash of empire/target/op-type, matching the pattern already used for combat resolution — two independent `.next()` draws preserve the intended 70% success and 50/50 detected-vs-failed odds. Scope boundary: only the two identified engine call sites were touched; `Math.random()` usage in test files was left as-is since test fixtures are not part of campaign-reproducibility guarantees.

### U-210 · HUMAN UAT: Phase 1 Alpha sign-off — `status: BLOCKED(human UAT)` · `coder: human` · `after: U-110, T-201, T-202, T-203, T-204, T-205`
The full Phase 1 checkpoint from CODING-PLAN ("Phase 1 Complete — Internal Alpha UAT"): one continuous campaign exercising expansion, economy, military, trade, diplomacy, research, achievements, save/load across sessions, and 10+ cycles of bot activity with a Reckoning. Also spot-check the never-played Phase 2 systems already built (installations, syndicate, covert ops — CODING-PLAN 2.1–2.3) since M3 builds on them. File follow-up tasks for everything that feels wrong; flip to DONE when the alpha is genuinely playable.
**Accept:** human sign-off recorded here (date + notes); status flipped to DONE by a human commit.

---

## M3 — Depth Systems (Phase 2 completion; gated on alpha sign-off)

### T-301 · Engine: blockade actions + starvation — `status: TODO` · `coder: fable` · `after: U-210`
Complete CODING-PLAN 2.4 on top of the existing `calculateBlockadeEffect` in `src/engine/combat/combat-resolver.ts`: `blockade` and `break-blockade` player actions in the cycle processor, blockade state tracked on the system, income suppression and escalating food-deficit starvation applied each cycle while blockaded, and bots able to blockade via the bot engine. Blockade-break resolves as fleet-engagement combat.
**Accept:** vitest covers: blockaded system's income suppressed over multiple cycles; starvation escalates with consecutive blockaded cycles; break-blockade combat lifts the blockade on victory and not on defeat; bot blockade action resolves during a cycle; full suite green.

### T-302 · UI: blockade visibility — `status: TODO` · `coder: opus` · `after: T-301`
Blockaded systems get a distinct indicator on `StarMap` and a BLOCKADED status line in `SystemPanel` with blockade/break-blockade actions wired through `App.tsx` (attack-pattern from T-102).
**Accept:** component tests assert the map indicator and panel status render from a blockaded-system fixture and the two actions fire with the right ids; full suite green.

### T-303 · UI: doctrine draft modal at the Reckoning — `status: TODO` · `coder: opus` · `after: U-210`
CODING-PLAN 2.5: when `checkResearchDraftTrigger` (in `src/engine/research/research-engine.ts`) fires during a committed cycle, present a card-selection modal (doctrine or specialisation choices as LCARS cards) instead of relying on the Research Panel; selection dispatches the existing `select-doctrine`/`select-specialization` actions. Research Panel remains the fallback view of the current state.
**Accept:** a component test renders the modal from a draft-trigger fixture, asserts one card per available choice, and selection fires the correct action id; modal does not render absent a trigger; full suite green.

### T-304 · UI: market & galaxy event notifications — `status: TODO` · `coder: opus` · `after: U-210`
CODING-PLAN 2.8 surface work: market events (famine etc., already generated engine-side) and achievement/galaxy-response events must reach the player. Extend `src/ui/CycleReport.tsx` to group and render these event categories with distinct styling, and add a compact "last cycle" event ticker to the HUD.
**Accept:** component tests render the report from a fixture containing market + achievement events and assert both groups appear with their details; ticker shows the most recent events; full suite green.

### T-305 · Engine: achievement galaxy responses + anti-snowball — `status: TODO` · `coder: fable` · `after: U-210`
CODING-PLAN 2.6: every one of the 9 achievements, when earned, must trigger its distinct galaxy response (title event, bot reaction per archetype), and coalition/aggression pressure must scale with proximity to achievement completion (anti-snowball). Audit `src/engine/achievement/achievement-checker.ts` + bot engine for what exists; implement the gaps; document each achievement→response mapping in the code.
**Accept:** vitest asserts each of the 9 achievements produces its specific response event when earned, and bot coalition-formation probability increases monotonically across low/mid/high completion-proximity fixtures; full suite green.

### T-306 · Engine: long-term betrayal memory — `status: TODO` · `coder: opus` · `after: U-210`
CODING-PLAN 2.7: bots remember pact violations for 30+ cycles (existing pruning in `pruneOldRelationships` must not erase betrayal records early), betrayal history weighs on pact acceptance, and reputation is surfaced in `DiplomacyPanel` per empire.
**Accept:** vitest asserts a violation still influences the offending empire's pact-acceptance odds 30 cycles later while ordinary relationship noise is pruned; component test shows the reputation readout from a fixture; full suite green.

### U-310 · HUMAN UAT: depth-systems playthrough — `status: BLOCKED(human UAT)` · `coder: human` · `after: T-302, T-303, T-304, T-305, T-306`
Closed-beta checkpoint from CODING-PLAN Phase 2: a long campaign chasing at least two different achievements, using blockades, reacting to a draft modal, profiting from a market event, and confirming a betrayed bot stays hostile. File follow-ups; flip to DONE.
**Accept:** human sign-off recorded here (date + notes); status flipped to DONE by a human commit.

---

## M4 — Hardening

### T-401 · Long-campaign integration + performance test — `status: TODO` · `coder: opus` · `after: U-310`
CODING-PLAN 3.6 groundwork: an integration test simulating 100+ full cycles at full scale (100 empires, 250 systems) asserting no thrown errors, no `committed:false` cycles, resource/unit invariants hold (no negative reserves, no orphaned unit ids), and mean cycle time under 1s on the test machine. Profile and fix hot paths if the budget is missed.
**Accept:** the new test lives in `src/engine/integration/`, runs in the default suite, asserts the invariants above and the <1s mean cycle budget; full suite green.

---

## Deliberately deferred (do not re-scope into tasks above)

- **LLM Nemesis bots** (CODING-PLAN 3.3) — needs live-model wiring and cost decisions; human-led.
- **100 named bot personas, scenario packs** (3.4/3.5) — content authoring, after beta.
- **Balance pass, in-game help, Steam packaging, LCARS polish pass** (Phase 4 + 2.10) — post-beta, taste-driven.
- **Multi-cycle colonisation** — current instant-claim is a recorded deviation (CODING-PLAN 1.6); revisit only if UAT says expansion feels weightless.
- **Idle rank-climb balance** (U-110 finding 4, residual) — powerScore = `systems×10 + credits×0.1 + researchTier×20` lets passive credit growth raise relative rank; M1.5 makes passing explicit but the scoring itself is a balance-pass concern.
- **Hardcoded `SEED = 42` in App.tsx** — every campaign generates the identical galaxy; needs a per-campaign seed (UI change + campaign options).
- **Attack unit-location validation** — the `attack` action builds the force from supplied `unitIds` without checking where they are stationed (units effectively teleport); design decision needed on staging/range rules.
